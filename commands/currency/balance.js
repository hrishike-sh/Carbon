const { Message, EmbedBuilder } = require('discord.js');
module.exports = {
  name: 'balance',
  aliases: ['bal'],
  cooldown: 1,
  /**
   *
   * @param {Message} message
   * @param {String[]} args
   */
  async execute(message, args) {}
};
