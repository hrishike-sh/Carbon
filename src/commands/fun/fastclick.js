const { Message, EmbedBuilder, ActionRowBuilder } = require('discord.js');

module.exports = {
  name: 'fastclick',
  aliases: ['fastclick'],
  roles: [],
  /**
   *
   * @param {Message} message
   * @param {String[]} args
   * @param {Client} client
   */
  async execute(message, args, client) {}
};
